{
  "name": "taskboard-assessment",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:api": "turbo run test --filter=api",
    "test:ui": "turbo run test:e2e --filter=web"
  },
  "devDependencies": {
    "@turbo/gen": "^1.10.12",
    "turbo": "^1.10.12"
  },
  "packageManager": "pnpm@8.15.6",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.15.0",
    "pnpm": "^10.15.1"
  }
}
